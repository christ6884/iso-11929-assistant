{
  "name": "iso11929-assistant",
  "version": "2.2.7",
  "description": "A web application to assist in understanding and applying the ISO 11929 standard for nuclear measurements.",
  "main": "electron/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "rimraf dist && esbuild index.tsx --bundle --outfile=dist/bundle.js --platform=browser --target=es2020 --jsx=automatic && copyfiles index.html manifest.json service-worker.js icons/**/* dist",
    "dist": "npm run build && electron-builder"
  },
  "author": "PHAM Christophe, UNITECH Service Group",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "copyfiles": "^2.4.1",
    "electron": "^23.1.1",
    "electron-builder": "^23.6.0",
    "esbuild": "^0.17.10",
    "gh-pages": "^5.0.0",
    "rimraf": "^4.1.2",
    "tailwindcss": "^3.2.7",
    "typescript": "^4.9.5"
  },
  "build": {
    "appId": "com.unitech.iso11929-assistant",
    "productName": "ISO 11929 Assistant",
    "files": [
      "dist/**/*",
      "electron/**/*"
    ],
    "directories": {
      "output": "../release_builds"
    }
  }
}